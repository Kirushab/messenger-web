# v391 validation

Проверено в рабочем окружении перед упаковкой:

- `node --check public/sw.js` — успешно.
- Поведенческий тест service worker: активный чат подавляет push, другой чат показывает push, видимый звонок не дублируется, клик открывает нужный маршрут — успешно.
- TypeScript parser для всех изменённых `.ts/.tsx` и Edge Function — синтаксических ошибок нет.
- `npm run check:tokens` — успешно.
- `npm run scan:secrets` — успешно.
- Целостность итогового ZIP проверяется командой `unzip -t` после упаковки.

Полный `npm ci` / `npm run verify:release` в этом окружении не завершился: внутреннее npm-зеркало трижды вернуло HTTP 503 при загрузке `zustand-4.5.7.tgz`. Это внешний сбой установки зависимостей, а не ошибка исходников; после восстановления registry нужно запустить `npm ci && npm run verify:release` локально или в CI.
