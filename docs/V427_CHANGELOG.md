# v427 — spoiler reveal + call navigation polish

- Hidden/spoiler messages now reveal with a softer blur/cover transition.
- A revealed spoiler is automatically hidden again after its message fully leaves the chat viewport.
- Active-call Back now returns to the linked chat without ending the call.
- Minimized active calls use a compact top floating call bar with reopen and hang-up controls.
- The protected-connection pill now matches the Back pill height and visual weight.
- No SQL migration is required.
