# v394 validation

- Проверены изменения JSX/TypeScript в `CreatePost.tsx`, `Appearance.tsx` и `MapPage.tsx`.
- Проверены взаимоисключающие условия отображения нижних панелей карты.
- Запущены доступные проверки дизайн-токенов и секретов.
- Полная dependency-backed сборка считается подтверждённой только после успешного `npm run verify:release` локально или в CI.
