-- ============================================================
-- 121_attachment_dimensions.sql  (v63)
-- Накатить в Supabase SQL Editor.
--
-- Размеры медиа-вложений (фото/видео), известные на момент отправки.
-- Нужны, чтобы в чате резервировать ТОЧНЫЙ aspect-ratio пузыря ДО загрузки
-- файла — иначе при подгрузке медиа лента «прыгает» (квадрат → реальный аспект).
--
-- Идемпотентно. Старые вложения остаются с NULL (фронтенд откатывается на
-- прежнее поведение с кэшем аспекта).
-- ============================================================

alter table public.file_attachments add column if not exists width  integer;
alter table public.file_attachments add column if not exists height integer;
